import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <React.StrictMode>
    <h1>Placeholder app</h1>
    <p>This Placeholder app was created in order to use Storybook to test the atomic components for the npm package.</p>
  </React.StrictMode>,
  document.getElementById('root'),
);
